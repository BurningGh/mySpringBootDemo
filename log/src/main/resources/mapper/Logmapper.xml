<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.log.dao.LogDao">
    <insert id="insert" parameterType="Log">
		insert into log(title, `action`, method, url,ip, param)
        values (#{title}, #{action}, #{method}, #{url}, #{ip},#{param})
	</insert>

</mapper> 